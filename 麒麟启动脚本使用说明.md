# AgentMatrix 麒麟/Linux 一键启动脚本使用说明

## 📦 文件位置

启动脚本: `c:\Users\王森\Desktop\AgentMatrix\start_ky麒麟.sh`

## 🚀 使用方法

### 方法一：图形界面操作（推荐新手）

1. 将整个 `AgentMatrix` 文件夹复制到麒麟系统的电脑上
2. 右键点击 `start_ky麒麟.sh` 文件
3. 选择 "属性" → "权限" → 勾选 "允许以程序执行"
4. 双击 `start_ky麒麟.sh` 文件
5. 选择 "在终端中运行" 或 "运行"

### 方法二：命令行操作

1. 打开终端（Ctrl+Alt+T）
2. 切换到脚本所在目录：
   ```bash
   cd /path/to/AgentMatrix
   ```

3. 添加执行权限：
   ```bash
   chmod +x start_ky麒麟.sh
   ```

4. 运行脚本：
   ```bash
   ./start_ky麒麟.sh
   ```

5. 可选：跟踪日志输出
   ```bash
   ./start_ky麒麟.sh --follow
   # 或
   ./start_ky麒麟.sh -f
   ```

## ✨ 脚本功能

启动脚本会自动执行以下操作：

1. ✅ 检查Python环境（需要Python 3.8+）
2. ✅ 检查Node.js环境（需要Node.js 16+）
3. ✅ 创建Python虚拟环境（推荐）
4. ✅ 安装后端依赖（FastAPI、Uvicorn等）
5. ✅ 安装前端依赖（Next.js等）
6. ✅ 检查并启动Ollama服务
7. ✅ 启动后端服务（端口8000）
8. ✅ 启动前端服务（端口3000）
9. ✅ 显示服务访问地址

## 🔧 前置要求

### Ubuntu/Debian/统信UOS/银河麒麟

```bash
# 安装Python3和pip
sudo apt-get update
sudo apt-get install python3 python3-pip python3-venv

# 安装Node.js（推荐使用NodeSource仓库获取最新版本）
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# 安装Ollama
curl -fsSL https://ollama.com/install.sh | sh
```

### CentOS/RHEL/中标麒麟

```bash
# 安装Python3和pip
sudo yum install python3 python3-pip python3-venv

# 安装Node.js
curl -fsSL https://rpm.nodesource.com/setup_18.x | sudo bash -
sudo yum install -y nodejs

# 安装Ollama
curl -fsSL https://ollama.com/install.sh | sh
```

## 📋 服务地址

启动成功后，服务地址如下：

| 服务 | 地址 | 说明 |
|------|------|------|
| 前端界面 | http://localhost:3000 | Web界面 |
| 后端API | http://localhost:8000 | REST API |
| API文档 | http://localhost:8000/docs | Swagger文档 |
| Ollama | http://localhost:11434 | 本地模型API |

## 🛑 停止服务

### 方法一：在终端中按 Ctrl+C

如果在终端中运行脚本，按 Ctrl+C 可以停止脚本，但服务会继续在后台运行。

### 方法二：使用停止命令

```bash
# 停止所有AgentMatrix相关服务
pkill -f 'uvicorn|next'

# 或者逐个停止
# 停止后端
pkill -f 'uvicorn.*8000'

# 停止前端
pkill -f 'next.*3000'

# 停止Ollama
pkill ollama
```

### 方法三：使用kill命令

```bash
# 查找进程ID
ps aux | grep -E 'uvicorn|next'

# 停止进程（将PID替换为实际进程ID）
kill <后端进程ID>
kill <前端进程ID>
```

## 📝 日志文件

脚本运行过程中会生成以下日志文件：

- `/tmp/agentmatrix_backend.log` - 后端服务日志
- `/tmp/agentmatrix_frontend.log` - 前端服务日志
- `/tmp/ollama.log` - Ollama服务日志

查看日志：
```bash
# 查看后端日志
tail -f /tmp/agentmatrix_backend.log

# 查看前端日志
tail -f /tmp/agentmatrix_frontend.log

# 查看所有日志
tail -f /tmp/agentmatrix_*.log
```

## 🔍 故障排查

### 问题1：权限不足

```bash
# 解决方法：添加执行权限
chmod +x start_ky麒麟.sh
```

### 问题2：端口被占用

```bash
# 查看端口占用情况
sudo lsof -i :8000   # 检查后端端口
sudo lsof -i :3000   # 检查前端端口
sudo lsof -i :11434  # 检查Ollama端口

# 停止占用端口的进程
sudo kill <进程ID>
```

### 问题3：依赖安装失败

```bash
# 手动安装Python依赖
cd backend
pip3 install --user pydantic pydantic-settings fastapi uvicorn httpx python-dotenv python-socketio sqlalchemy

# 手动安装Node依赖
cd ../frontend
npm install
```

### 问题4：Ollama模型不可用

```bash
# 查看可用模型
ollama list

# 拉取模型（如果需要）
ollama pull qwen2.5:1.5b
ollama pull phi4-mini:3.8b
```

### 问题5：网络问题导致依赖下载失败

```bash
# 设置国内镜像源
# npm
npm config set registry https://registry.npmmirror.com

# pip
pip3 install -i https://pypi.tuna.tsinghua.edu.cn/simple pydantic pydantic-settings fastapi uvicorn httpx python-dotenv python-socketio sqlalchemy
```

## 🎯 快速命令参考

```bash
# 完整启动
./start_ky麒麟.sh

# 启动并跟踪日志
./start_ky麒麟.sh -f

# 查看后端状态
curl http://localhost:8000/health

# 查看Ollama模型
ollama list

# 查看日志
tail -f /tmp/agentmatrix_*.log

# 停止所有服务
pkill -f 'uvicorn|next'
```

## 📞 支持

如果遇到问题，请检查：

1. Python版本是否 >= 3.8
2. Node.js版本是否 >= 16
3. 端口是否被占用
4. 网络连接是否正常
5. 磁盘空间是否充足

## 📄 许可证

本项目遵循 AgentMatrix 开源协议。

---

**享受使用！** 🎉
